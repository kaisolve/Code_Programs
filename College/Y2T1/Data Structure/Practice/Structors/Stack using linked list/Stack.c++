#include <bits/stdc++.h>
#include "Stack.h"
using namespace std;

int main(){
    Stack st;
    node *head=NULL;
    st.push(50);
    st.push(30);
    st.push(325);
    st.pop();
    // st.display();
    st.peek();
}

